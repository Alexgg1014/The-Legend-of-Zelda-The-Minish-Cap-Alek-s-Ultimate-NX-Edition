# Alek's Ultimate NX Edition v0.1.2

A minimal sword hitbox hotfix. Everything from v0.1.1 is preserved — this
release changes one collision detail, nothing else.

## Fixed

- Fixed an issue where sword collision could be mirrored to the wrong side
  during certain right-facing attacks.
- Improved native-port gameplay fidelity.

## Installation

Copy the release to `/switch/tmc/`, provide your own legally obtained
compatible USA ROM, and launch in application mode.

No save or configuration migration is required. Existing `tmc.sav`,
`autosave.bin` and `config.json` are used as-is.

## Verification

    SHA-256  fe9921882507bcff3c33b45b9109aedaa908433a85113d2aa98a80b9d4331fe6
             tmc_aleks_ultimate_nx_v0.1.2.nro
